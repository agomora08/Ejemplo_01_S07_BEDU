# El siguiente 'script' es de carácter demostrativo solamente; con él se busca
# ejemplificar la capacidad que tiene RStudio Cloud para subir archivos R a un
# repositorio de GitHub existente (función "PUSH").

print("¡Demostración de cómo subir archivos funcionales a GitHub desde RStudio Cloud!")

(example.df <- data.frame(x = letters, y = seq(1, length(letters))))